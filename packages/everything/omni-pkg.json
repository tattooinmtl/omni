{
  "name": "everything",
  "version": "1.0.0",
  "type": "mcp",
  "description": "MCP 'everything' reference server (echo, add, longRunningOperation, …) — great for testing the mcp proxy.",
  "mcp": {
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-everything"]
  }
}
